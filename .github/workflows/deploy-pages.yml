name: Legacy Pages (disabled)
on:
  workflow_dispatch:
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages is disabled. Fiscal Control is deployed through Cloudflare."
