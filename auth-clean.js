(()=>{'use strict';const root=document.getElementById('auth'),form=document.getElementById('loginForm'),err=document.getElementById('loginError');form.addEventListener('submit',async e=>{e.preventDefault();err.textContent='';const b=form.querySelector('button');b.disabled=true;b.textContent='Entrando…';try{const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify({username:document.getElementById('username').value.trim(),password:document.getElementById('password').value})});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Não foi possível entrar.');if(!d.user)throw Error('Sessão inválida.');window.FC_CLEAN={user:d.user};root.classList.add('hidden');document.getElementById('app').style.display='block';window.render()}catch(x){err.textContent=x.message}finally{b.disabled=false;b.textContent='Entrar'}});window.CLEAN_LOGOUT=()=>{fetch('/api/logout',{method:'POST',credentials:'same-origin'}).catch(()=>{});location.reload()}})();